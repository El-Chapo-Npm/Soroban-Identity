# Soroban Identity starter

```bash
npx degit El-Chapo-Npm/Soroban-Identity/docs/tutorials/templates/starter my-app
cd my-app && cp .env.example .env   # fill in testnet contract IDs
npm install && npm start
```
